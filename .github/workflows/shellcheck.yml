name: DevForgeKit ShellCheck

on:
  push:
    paths:
      - "**/*.sh"
      - "bootstrap.sh"
      - "devforgekit"
      - ".github/workflows/shellcheck.yml"
  pull_request:
    paths:
      - "**/*.sh"
      - "bootstrap.sh"
      - "devforgekit"
      - ".github/workflows/shellcheck.yml"

permissions:
  contents: read

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Run ShellCheck
        run: |
          shopt -s globstar
          shellcheck -x bootstrap.sh devforgekit scripts/*.sh
