schemaVersion: 1
name: docker
description: Container runtime and CLI for building/running containers
category: containers
platforms: [macos, linux, windows]
architectures: [intel, apple-silicon, x64, arm64]
variants:
  - id: docker-desktop
    label: Docker Desktop (official, GUI + CLI)
    install: { method: brew-cask, id: docker }
    platformInstall:
      macos: { method: brew-cask, id: docker }
      windows: { method: winget, id: Docker.DockerDesktop }
  - id: colima
    label: Colima (lightweight, open-source, CLI-only)
    install: { method: brew-formula, id: colima }
  - id: docker-engine
    label: Docker Engine (Linux, CLI-only)
    install: { method: apt, id: docker-ce }
dependencies: []
validate: docker info
versionCommand: docker --version
repair: brew reinstall --cask docker
update: "brew upgrade --cask docker"
uninstall: { method: brew-cask, id: docker }
post_install:
  - "open -a Docker"
recommendedAlternatives: [podman]
homepage: "https://www.docker.com"
repository: "https://github.com/docker/cli"
license: Apache-2.0
documentation: "https://docs.docker.com"
maintainer: DevForgeKit Registry
tags: [containers, devops, virtualization]
aliases: [docker-desktop]
stability: stable
lastVerified: "2026-07-05"
