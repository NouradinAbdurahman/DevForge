# Backend profile

Server-side/infra development: PostgreSQL, MySQL, Redis, Supabase CLI,
AWS CLI, kubectl, Helm, Terraform, and matching VS Code extensions
(Docker, Kubernetes, Terraform, SQL Tools).

Use with:

```bash
./bootstrap.sh --profile backend
# or
./dev install --profile backend
```
