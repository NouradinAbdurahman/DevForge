# Custom profile

A blank starting point - `profiles/custom/Brewfile` ships empty. Add your
own `brew`/`cask`/`vscode`/`npm` lines to it (copy from the root
[Brewfile](../../Brewfile) or write new ones), then:

```bash
./bootstrap.sh --profile custom
# or
./dev install --profile custom
```
